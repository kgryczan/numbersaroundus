document.addEventListener('DOMContentLoaded',()=>{const article=document.querySelector('main.content');if(article&&!article.querySelector('.nau-hero')&&document.body.scrollHeight>innerHeight*1.4){const bar=document.createElement('div');bar.className='reading-progress';bar.setAttribute('aria-hidden','true');document.body.append(bar);const update=()=>{const max=document.documentElement.scrollHeight-innerHeight;bar.style.width=`${max>0?Math.min(100,scrollY/max*100):0}%`};addEventListener('scroll',update,{passive:true});addEventListener('resize',update);update()}const orbit=document.querySelector('.nau-orbit');const core=document.querySelector('.nau-core');if(orbit&&core&&!matchMedia('(prefers-reduced-motion: reduce)').matches){orbit.addEventListener('pointermove',e=>{const r=orbit.getBoundingClientRect();core.style.transform=`translate(${(e.clientX-r.left-r.width/2)/35}px,${(e.clientY-r.top-r.height/2)/35}px)`});orbit.addEventListener('pointerleave',()=>core.style.transform='')}});
