---
title: "Excel BI - Excel Challenge 834"
description: "🔰 Group the alphabets row wise and sum the values beneath them."
categories: [excel-challenges, excel-formulas]
tags: [excelbi, excel, challenge]
format: 
  html:
    code-fold: false
date: "2026-03-24"
editor: 
  markdown: 
    wrap: sentence
---

![Illustration for Excel BI - Excel Challenge 834](/challenges/images/exc834.jpg)

## Challenge Description

🔰 Group the alphabets row wise and sum the values beneath them.

## Solutions

::: panel-tabset
### R Solution

```{r}
#| eval: false

library(tidyverse)
library(readxl)

path = "Excel/800-899/834/834 Group Rowwise.xlsx"
input = read_excel(path, range = "A2:F10")
test  = read_excel(path, range = "H2:I6")

result = input %>%
  as.matrix() %>%
  apply(1, function(x) {
    if (which(input == x[1], arr.ind = TRUE)[1, "row"] %% 2 == 0) {
      paste(x %>% na.omit(), collapse = "+")
    } else {
      paste(x %>% na.omit(), collapse = "")
    }
  }) %>%
  matrix(ncol = 2, byrow = TRUE) %>%
  as_tibble() %>%
  mutate(V2 = map_int(V2, ~ eval(parse(text = .x)))) %>%
  set_names(c("Group", "Sum"))

all.equal(result, test)
# [1] TRUE
```

### R Analysis

- **Logic**: Read the workbook ranges needed for the challenge; Derive the required intermediate columns.
- **Strengths**: The code maps the workbook rule into a compact, reproducible pipeline.
- **Areas for Improvement**: The solution assumes the workbook layout and selected ranges remain stable, so any structural change in the sheet would require small adjustments.
- **Gem**: The elegant part is how little code is needed once the correct intermediate representation is chosen.

### Python Solution

```{python}
#| eval: false

import pandas as pd
import numpy as np

path = "800-899/834/834 Group Rowwise.xlsx"
input = pd.read_excel(path, usecols="A:F", skiprows=1, nrows=9)
test = pd.read_excel(path, usecols="H:I", skiprows=1, nrows=4)
input_matrix = input.to_numpy()
result = pd.DataFrame({
    "Group": ["".join([str(x) for x in row if pd.notna(x)]) for row in input_matrix[::2]],
    "Sum": [eval("+".join([str(x) for x in row if pd.notna(x)])) for row in input_matrix[1::2]]
})

print(result.equals(test))
# True
```

### Python

The Python version keeps the algorithm explicit, which helps when the challenge depends on a greedy or iterative rule.

## **Difficulty Level**

**Easy / Medium**

The business rule is clear, though the workbook still needs a few transformation steps to reach the expected output.

### Related Challenges

- [Excel Challenge 835](/challenges/excelbi/Excel835.html)
- [Excel Challenge 836](/challenges/excelbi/Excel836.html)
- [Excel Challenge 837](/challenges/excelbi/Excel837.html)
