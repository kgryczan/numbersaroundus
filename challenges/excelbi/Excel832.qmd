---
title: "Excel BI - Excel Challenge 832"
description: "🔰 String Answer Expected Nosta*lg*ia Nosta Re*sili*en*ce Rile E**loqu**ence oqnce Euphoria********* Euphori"
categories: [excel-challenges, excel-formulas]
tags: [excelbi, excel, challenge]
format: 
  html:
    code-fold: false
date: "2026-03-24"
editor: 
  markdown: 
    wrap: sentence
---

![Illustration for Excel BI - Excel Challenge 832](/challenges/images/placeholder-excelbi.png)

## Challenge Description

🔰 String Answer Expected Nosta*lg*ia Nosta Re*sili*en*ce Rile E**loqu**ence oqnce Euphoria********* Euphori

## Solutions

::: panel-tabset
### R Solution

```{r}
#| eval: false

library(tidyverse)
library(readxl)

path = "Excel/800-899/832/832 Delete Characters Around Asterisks.xlsx"
input = read_excel(path, range = "A1:A10")
test  = read_excel(path, range = "B1:B10") %>%
  mutate(`Answer Expected` = replace_na(`Answer Expected`, ""))

result = input %>%
  mutate(result = str_remove_all(String, "(?<=\\*).|.(?=\\*)|\\*"))

all.equal(test$`Answer Expected`, result$result, check.attributes = F)
```

### R Analysis

- **Logic**: Read the workbook ranges needed for the challenge; Derive the required intermediate columns.
- **Strengths**: The code maps the workbook rule into a compact, reproducible pipeline.
- **Areas for Improvement**: The solution assumes the workbook layout and selected ranges remain stable, so any structural change in the sheet would require small adjustments.
- **Gem**: The elegant part is how little code is needed once the correct intermediate representation is chosen.

### Python Solution

```{python}
#| eval: false

import pandas as pd
import re
import numpy as np

path = "800-899/832/832 Delete Characters Around Asterisks.xlsx"
input = pd.read_excel(path, usecols="A", nrows=10)
test = pd.read_excel(path, usecols="B", nrows=10).fillna("")

input['Result'] = input['String'].str.replace(r'(?<=\*).|.(?=\*)|\*', '', regex=True)

print(input['Result'].equals(test['Answer Expected'])) # True
```

### Python

The Python version mirrors the same workbook logic with a concise, direct implementation.

## **Difficulty Level**

**Easy / Medium**

The business rule is clear, though the workbook still needs a few transformation steps to reach the expected output.

### Related Challenges

- [Excel Challenge 833](/challenges/excelbi/Excel833.html)
- [Excel Challenge 834](/challenges/excelbi/Excel834.html)
- [Excel Challenge 835](/challenges/excelbi/Excel835.html)
