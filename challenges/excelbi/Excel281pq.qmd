---
title: "Excel BI - PowerQuery Challenge 281"
description: "Extract the capitals of different states. States appear after dash ('-') and capitals are followed by '(C)'."
categories: [excel-challenges, power-query]
tags: [excelbi, power-query, challenge]
format: 
  html:
    code-fold: false
date: "2026-03-24"
editor: 
  markdown: 
    wrap: sentence
---

![Illustration for Excel BI - PowerQuery Challenge 281](/challenges/images/exc281pq.jpg)

## Challenge Description

Extract the capitals of different states. States appear after dash ("-") and capitals are followed by "(C)".

## Solutions

::: panel-tabset
### R Solution

```{r}
#| eval: false

library(tidyverse)
library(readxl)

path = "Power Query/PQ_Challenge_281.xlsx"
input = read_excel(path, range = "A1:A9")
test = read_excel(path, range = "C1:D10")


# Solution 1
result = input %>%
  separate_rows(Data, sep = ", ") %>%
  filter(str_detect(Data, "\\(C\\)")) %>%
  separate(Data, into = c("State", "Capital"), sep = " \\(C\\) - ")

# Solution 2
result <- input %>%
  separate_rows(Data, sep = ", ") %>%
  extract(Data, c("State", "Capital"), "^(.*?) \\(C\\) - (.*)$") %>%
  drop_na()
```

### R Analysis

-   **Logic**:

    -   Reads the workbook range needed for the challenge

    -   Uses direct pattern parsing where the workbook encodes logic in text

-   **Strengths**:

    -   The R solution stays close to the workbook logic and keeps the transformation compact.

-   **Areas for Improvement**:

    -   The code assumes the workbook layout and selected ranges remain stable.

-   **Gem**:

    -   The best part of the solution is choosing the right intermediate shape before formatting the final output.

### Python Solution

```{python}
#| eval: false

import pandas as pd

path = "PQ_Challenge_281.xlsx"
input = pd.read_excel(path, usecols="A", nrows=8)
test = pd.read_excel(path, usecols="C:D", nrows=10)

result = input.assign(Data=input['Data'].str.split(',')).explode('Data')
result[['Capital', 'State']] = result['Data'].str.split(r' \(C\) - ', expand=True)
result = result.drop(columns=['Data']).sort_values(by=['State'])
result = result[["State", "Capital"]].reset_index(drop=True)
result = result.dropna()


print(result)
print(test)
```

### Python

-   **Logic**:

    -   Reads the workbook range needed for the challenge

    -   Builds helper columns that drive the final output

-   **Strengths**:

    -   The Python version follows the same workbook rule in a direct pandas-oriented implementation.

-   **Areas for Improvement**:

    -   As with the R version, any workbook layout change would require small adjustments.

-   **Gem**:

    -   The implementation stays close to the source challenge instead of adding unnecessary abstraction.
:::

## **Difficulty Level**

This task is **moderate**:

-   It combines reshaping, grouping, or parsing steps that are common in Power Query style problems.

-   The main challenge is reproducing the workbook output structure exactly.

### Related Challenges

Explore more challenges from the same series:

-   [Excel BI - PowerQuery Challenge 280](Excel280pq.qmd)
-   [Excel BI - Excel Challenge 661](Excel661.qmd)
